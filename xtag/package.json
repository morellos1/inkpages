{
  "name": "inkpages-xtag",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension: one-click tagging of artist profiles on X into the inkpages directory.",
  "type": "module",
  "scripts": {
    "build": "node esbuild.mjs",
    "watch": "node esbuild.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.21.0",
    "typescript": "^5.4.0"
  }
}
